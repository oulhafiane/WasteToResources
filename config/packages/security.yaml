security:
    encoders:
        App\Entity\Utilisateur:
            algorithm: argon2i

    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_collecteur_provider:
            entity:
                class: App\Entity\Collecteur
                property: email
    firewalls:
        register:
            pattern:    ^/collecteur_api/nouveau
            stateless:  true
            anonymous: true
        login:
            pattern:    ^/collecteur_api/auth
            stateless:  true
            anonymous: true
            json_login:
                check_path:         /collecteur_api/auth
                success_handler:    lexik_jwt_authentication.handler.authentication_success
                failure_handler:    lexik_jwt_authentication.handler.authentication_failure
        api:
            pattern:    ^/collecteur_api
            stateless:  true
            provider:   app_collecteur_provider
            guard:
                authenticators:
                    - lexik_jwt_authentication.jwt_token_authenticator
        main:
            anonymous: true

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
         - { path: ^/collecteur_api/nouveau, roles: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/collecteur_api/auth, roles: IS_AUTHENTICATED_ANONYMOUSLY }
         - { path: ^/collecteur_api, roles: IS_AUTHENTICATED_FULLY }
